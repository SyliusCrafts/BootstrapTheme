{% macro simple(paginator, options) %}
    {% if paginator.haveToPaginate() %}
        {{ pagerfanta(paginator, 'twitter_bootstrap5', options|default({
            'prev_message': 'sylius.ui.previous'|trans, 
            'next_message': 'sylius.ui.next'|trans
        })) }}
    {% endif %}
{% endmacro %}
